const App = () => (
    // These are the props/properties to use in the Components
    <div>
        <FirstComponent />

        <NamedComponent name="RalphsCode" />
    </div>
  );
  
  ReactDOM.render(<App/>, document.getElementById("root"));

  // 